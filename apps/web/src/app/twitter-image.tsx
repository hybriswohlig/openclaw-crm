/**
 * Twitter Card mirrors opengraph-image.tsx for non-portal routes. Per-firma
 * twitter-image lives at (public)/s/[token]/twitter-image.tsx.
 */
export { default, alt, size, contentType, runtime } from "./opengraph-image";
